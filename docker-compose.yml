# Mac mini 上测试用：docker-compose up
# 需在项目根目录放 .env 或 .env.local（Supabase 等），或取消下面 env_file 注释
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    # 从项目根目录的 .env.local 读取 Supabase 等配置（必填，否则会报错）
    env_file: .env.local
